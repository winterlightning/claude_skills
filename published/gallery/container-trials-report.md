# Container pair trials

321 recorded pairings, 289 distinct layouts, 102 containers.

271 pairings have a clear raster clearance estimate. 26 open layouts and 24 crowded layouts require placement review.

All results are trial previews. Existing SOLO48 artwork was uniformly fitted, keeping a 4-unit stroke, without authoring SUB32 models or modifying either component. Raster clearance is an advisory estimate, not frozen composition validation or proof that internal content details satisfy SUB32 rules.

Visual review: every distinct layout inspected at 64px in light and dark themes. Whiteboard content moved away from the person; prohibition subjects kept centered and flagged because their strokes overlap the slash. Open books, narrow camera lenses, cross-shaped enclosures, and gift boxes are among the remaining crowded cases.

Provenance: every record includes pair ID, main/sub reference IDs, model keys, source SVG hashes, placement, and output hash. Progression publishes these separately as Trial preview and does not mark the pair generated or approved. A component edit, remapping, altered preview, or missing file hides the trial on the next catalog rebuild and shows a stale notice.

Checks: 5 Python catalog/provenance tests passed; existing container Progression grouping and role-isolation checks passed; JavaScript syntax check passed; all 321 published preview links and all output hashes verified.
