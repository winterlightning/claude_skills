"""A golfer twists with bent knees and crossed raised arms in front of the upper torso. The club shaft extends diagonally above the head to a small curved head at the upper left.

Overhead club and twisting stance retained. Overlapping arms reduced to one grip path; club head reduced to a short angled end.
"""
from ...keyshapes import Keyshape
from ._base import Solo48
SOURCE_ICON_ID = '756e0e62-d045-4387-8d15-670479377f26'
SOURCE_PATH = 'pictographic-primitives/sports/golf player_756e0e62-d045-4387-8d15-670479377f26.svg'
AUTHOR = 'gpt-6'

class GolferSwingingClub(Solo48):
    icon_id = 'golfer-swinging-club'
    keyshape = Keyshape.SQUARE
    semantic_role = "MAIN"
    semantic_kind = "noun"
    category = "sports"
    aliases = ()
    keywords = ('golf', 'golfer', 'club', 'swing', 'athlete', 'sport')

    def circle(self,name,x,y,r):
        self.add_arc(name+'-top',(x-r,y),(x+r,y),radius_x=r)
        self.add_arc(name+'-bottom',(x+r,y),(x-r,y),radius_x=r)
        self.add_contour(name,name+'-top',name+'-bottom',closed=True)

    def skeleton(self,branches):
        parts=[]
        for name,points in branches:
            members=[]
            for index,(a,b) in enumerate(zip(points,points[1:])):
                key=f'{name}-{index}';members.append(key)
                self.add_line(key,a,b);parts.append((key,a,b))
            if len(members)>1:self.add_contour(name,*members)
        for index,(a,p,q) in enumerate(parts):
            for b,r,s in parts[index+1:]:
                if p in (r,s) or q in (r,s):self.relate('connect',a,b)

    def rounded(self,name,x,y,w,h,r):
        pts=[(x+r,y),(x+w-r,y),(x+w,y+r),(x+w,y+h-r),(x+w-r,y+h),(x+r,y+h),(x,y+h-r),(x,y+r)]
        members=[]
        for index,a in enumerate(pts):
            b=pts[(index+1)%8];key=f'{name}-{index}';members.append(key)
            if index%2:self.add_arc(key,a,b,radius_x=r)
            else:self.add_line(key,a,b)
        self.add_contour(name,*members,closed=True)

    def weight(self,name,x,y,w,h,r):
        # Expose bar attachment nodes at the midpoint of each vertical wall.
        middle=y+h//2
        pts=[(x+r,y),(x+w-r,y),(x+w,y+r),(x+w,middle),(x+w,y+h-r),(x+w-r,y+h),(x+r,y+h),(x,y+h-r),(x,middle),(x,y+r)]
        members=[]
        for index,a in enumerate(pts):
            b=pts[(index+1)%len(pts)];key=f'{name}-{index}';members.append(key)
            if index in [1,4,6,9]:self.add_arc(key,a,b,radius_x=r)
            else:self.add_line(key,a,b)
        self.add_contour(name,*members,closed=True)

    def build(self):
        self.circle('head',12,19,3)
        self.skeleton([('club',[(6,6),(12,6),(42,18),(30,26)]),('arms',[(30,26),(22,30)]),('torso',[(22,30),(24,34)]),('left-leg',[(24,34),(14,42)]),('right-leg',[(24,34),(34,42)])])
