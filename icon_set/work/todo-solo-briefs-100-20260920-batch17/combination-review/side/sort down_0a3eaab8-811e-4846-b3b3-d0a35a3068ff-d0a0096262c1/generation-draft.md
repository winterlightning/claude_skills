# Sorting Rows with Downward Arrow — reference review

- source: `pictographic-primitives/_uncategorized_35/sort down_0a3eaab8-811e-4846-b3b3-d0a35a3068ff.svg`
- source UUID: `0a3eaab8-811e-4846-b3b3-d0a35a3068ff`
- classification: side_combination
- review: pending_human_review

The directional arrow adds a sorting action above separate list rows.

## Visual draft

Three horizontal rows sit below a detached downward arrow.

## 1. Three Horizontal List Rows (solo)

Three equal horizontal rows are vertically spaced and aligned at both ends. Exclude the arrow.

## 2. Downward Arrow (sub)

A straight upright shaft ends in an open downward arrowhead. Exclude all horizontal rows.

## Authoring handoff

You can try modifying a copy of the SVG reference to fit the icon design rules, or generate a new icon that matches the icon name. Either approach must follow the requested family's design rules and preserve the named subject's identity. Apply this only to the named component and exclude the other components. Keep the original reference unchanged.
