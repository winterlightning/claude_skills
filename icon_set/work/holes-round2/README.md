# Second hole-repair review

25 independent candidate models. All pass full per-icon validation with zero errors or warnings, including negative-space checks. The isolated 25-icon review build exits 0. Native light and dark images and enlarged before/after comparisons were visually inspected.

Original models have not been replaced; follow the established approval workflow. user-with-gear is excluded and horse-head-v2 already passes. mapping.json maps originals to candidates. review.html is self-contained. build/ holds the isolated review exports.

Larger changes are documented per card, including circular outlet housing, simplified skull eye/teeth details, one headset signal arc, and two crystal rays. No validation thresholds changed.

## Approval applied

All 25 approved geometries were promoted to their original models and the SOLO failure gallery was rebuilt. All 25 originals pass all checks. Earlier sources are preserved in approved-source-backups/. See post-approval-check.json for remaining library hole failures.
