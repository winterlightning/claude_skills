# Laptop with solid square marks

Reviewed at native 48px and enlarged size in both themes. Two small solid rounded squares remain vertically aligned and distinct. Screen, hinge and base remain readable; all clearance outside the intentional solid marks passes without warnings.

User-approved visual exception is recorded in exception.json and the Python module as VISUAL_EXCEPTION, bound to the exact SVG hash. The current publication exception handler supports SUB32 only; this record does not change SOLO48 automatic validation or claim a pipeline pass.

The four remaining findings measure opposite edges within each intentionally solid mark. No other errors or warnings remain.

Keyshape: SQUARE, preserving the previous taller laptop envelope. Construction: original reference and the prior laptop contour. Reduction: outlined screen boxes replaced by small solid rounded squares at the user’s explicit request.
