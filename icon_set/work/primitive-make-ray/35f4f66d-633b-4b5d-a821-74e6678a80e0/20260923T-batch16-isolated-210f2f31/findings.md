# User bust inside a frame with a top band.

SQUARE: Balanced composition uses visible extremes (4,4)–(44,44).

Circular head and mirrored shoulders remain clear, though head is small. Detached head bottom y=28 and shoulder crest y=36 yield exact 4-unit ink gap.

Omissions: Lower frame edge also serves as lower band; neck simplified to a detached head.

Inspected Lucide scan-face original and atomic-debug; used coherent contours and round joins.

```text
status: valid
```
