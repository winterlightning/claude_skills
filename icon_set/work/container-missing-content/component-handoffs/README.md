# Superseded routing drafts

The user explicitly authorized generating these complete combined subjects. These earlier split handoffs were not queued and no longer represent pending work. See ../generation-status.json for the generated results.
