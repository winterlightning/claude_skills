# Medical Prescription Document — reference review

- source: `pictographic-primitives/_uncategorized_31/prescription drug px 2_5a9d43a5-7519-4e64-81a0-64a7c06298cc.svg`
- source UUID: `5a9d43a5-7519-4e64-81a0-64a7c06298cc`
- classification: container_combination
- review: component_briefs_ready

A readable prescription character is placed on a scroll independently of its paper shape.

## Visual draft

A curled prescription sheet carries an R with a crossed diagonal leg above two short text lines.

## 1. Blank Prescription Scroll (container)

A tall paper sheet curls outward at the upper right and lower left, with two short generic text lines below. Exclude the prescription character.

## 2. Existing Typeface Layout ℞ (typeface)

Preserve exact readable text `℞`. Reuse preferred glyphs `symbol-prescription` from icon_set/typeface/glyphs.json. Place the existing prescription glyph above two generic text lines on the separately briefed scroll; the source shows its Rx-like crossed leg. Exclude all surrounding subjects and enclosures; do not trace replacement character geometry.

## Authoring handoff

You can try modifying a copy of the SVG reference to fit the icon design rules, or generate a new icon that matches the icon name. Either approach must follow the requested family's design rules and preserve the named subject's identity. Apply this only to the named component and exclude the other components. Keep the original reference unchanged.

Typeface components must follow the reuse instructions above, never new icon generation.
