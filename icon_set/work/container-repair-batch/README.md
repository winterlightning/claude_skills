# 37-container repair pass

Reviewed all 37 containers behind the 143 repair-preferred pair previews. Preserved all original models. Selected 36 independent container variants; kept the original concentric rings because a larger inner circle would crowd the outer ring.

135 of the 143 original native-32 failures pass after the repairs. Eight remain: three concentric-ring pairs, two camera pairs, and three sedan pairs. Their 24-unit simulations pass. These are redraw suggestions, not approved fractional-scale sub assets.

All selected models are valid with no warnings. Thirty focused geometry, placement, center and sizing tests pass. Compared all measurable paired subs in the captured baseline, including previously passing pairs: no regressions. Reviewed before/after compositions at native size in light and dark themes. Local Lucide original and atomic-debug references inspected for basket, store, sticky-note, truck, lock, video, circle, messages, film, fuel, gift, flag, factory, soap dispenser, book, mail, camera, milk, frame, cooking pot, tag, flask, search, car, notebook, smartphone, egg, scan, panels, cup and open lock construction.

The authoritative drawings are the Python modules in accepted.json. The author_variants.py script records initial scaffold designs, not subsequent refinements; do not rerun it to update the final modules. baseline.json preserves the exact pair/component snapshot tested. check_repairs.py and measured.json record pair-specific validation. Rendering scripts compare original and revised native geometry; 24-unit previews remain labeled prototypes.

The broad suite is not claimed to pass; earlier server integration checks were blocked by sandbox restrictions on local port binding. Targeted model build outcome is recorded in build.log.

Final catalog refresh selected flash-sub32-v3 in the two sedan pairs. Rechecked that geometry and centered the sedan content at (30,30); both new flash pairs and the heart pass the 24-unit simulation. The original pair definitions remain unchanged.
