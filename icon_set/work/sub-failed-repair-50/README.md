# First 50 current SUB32 failed-check repairs

34 repaired and activated; 16 unresolved. All 50 originals and prior Python models are preserved. Open `index.html` for original, before, after, centerline and native light/dark comparisons.

Selection: first 50 recorded failures that still belong to the sub family and fail the current model checks. The historical report also contains icons migrated to the symbol family; those were excluded. Review-only entries were excluded.

Validation: all 34 activated models passed the full export inspection without warnings, container-circle composition, and the targeted build. All original source UUIDs and linked source profiles are preserved. The 38 workflow-isolation, generation, metadata and existing centerline-regression tests passed with local socket access. Full unittest discovery was attempted but interrupted during expensive gallery staging after sandbox socket errors; it is not claimed to have passed. Workspace doctor reports zero tracked generated files.

34 active replacements update 41 profile links and 50 combination-pair records. Existing precomputed combined SVG caches were not regenerated in this task. Human approvals, manual artwork and server review state were not changed. Batch-local primitive skip decisions are in `primitive-status.json` and `primitive-status.sqlite3`.

Recorded remaining inventory: 272 failed sub-family profiles (16 unresolved here plus 256 untouched), and 12 sub-family review warnings. Across the historical 1532-profile inventory, including profiles now in the symbol family: 928 pass, 569 fail, 35 review. These are recorded totals, not a new full-library validation.

Construction references: local Lucide originals and atomic-debug drawings for arrows, bags, batteries, flask, bed, bell, fingerprint, eye, bone, books, briefcase, broken chain, beetle, building, bus, camera, car, cat and speech bubble. Subject parts and orientation come from the exact source originals. Human reference was inspected for the circular child head; no detached body is present. Deliberate asymmetry is retained in directional subjects, sloping buildings, broken links, hair part and chilli. No source parts were dropped from accepted repairs; invented older details were removed where the original does not contain them.

Audit files: `audit.json` gives all 50 outcomes, inventories and blockers; `release-qa.json` contains full export checks; `composition-checks.json` contains host-fit checks; `verification.json` records provenance/link checks. `activation-backup` stores pre-change metadata for recovery. Python originals under `icon_set/model/icons/sub/` are authoritative. Scripts here record the exploratory repair steps and must not be rerun as a bulk activation procedure.
