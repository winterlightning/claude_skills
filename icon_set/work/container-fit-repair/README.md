# Container native-SUB32 fit repair

Scope: preserve distinctive shapes; fix practical fit issues for the check mark, plus and heart at native 32 × 32, retaining 2 SVG units of clearance.

11 independent model variants were built successfully. All 33 revised sample combinations pass vector clearance, interior and canvas checks. Minimum revised clearance is approximately 2.9997 units. Originals are preserved; no default source selection has been changed.

35 placement-only corrections across 27 containers pass the same checks. Coordinates and source hashes are in fit-adjustments.json. These positions are sample-specific, not a promise that all 266 sub-icons will fit.

87 containers still have one or more unresolved sample combinations. The report lists them individually. A half-unit placement search is evidence of a found fit, not proof of impossibility when no fit was found. Intentional overlays remain separate.

Geometry: expanded browser/phone/frame/sign/display faces, retained paired corner radii and identifying furniture. The presentation-board revision omits its secondary lower cross brace to keep the larger panel, deep tray and legs clear. Other revised details are retained. Inspected Lucide app-window-mac, smartphone, tablet, watch, calendar, monitor and presentation originals/atomic-debug references. Reviewed native and enlarged drawings in light and dark themes.

Validation: all 11 model validations are valid; targeted container build exits 0; 24 focused geometry/placement tests pass. Full-suite execution was stopped after gallery integration setup errors; an isolated reproduction fails because this sandbox disallows binding the local test server (PermissionError). No claim of a full-suite pass.

Review index.html for the 68 before/after combinations. The original comparison gallery links to this report.

## Visual balance revision

Centered placement now has priority. The vase heart moves from (32, 38.5) to (32, 40.5); the heart-container plus moves from (38, 28.5) to (32, 35). Revalidated lower clearance bounds are 2.8810 and 2.6591 units. Other placement candidates were recomputed with horizontal centerline priority; off-axis candidates remain explicitly marked for visual review. Optical overrides are saved in the placement preferences file.
