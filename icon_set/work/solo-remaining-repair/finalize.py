from pathlib import Path
import sys,json,math
ROOT=Path(__file__).resolve().parents[3];sys.path.insert(0,str(ROOT))
SOURCE_ICON_ID=None
SOURCE_PATH='icon_set/work/solo-remaining-repair/before.json'
AUTHOR='gpt-6'
W=Path(__file__).parent;before=json.loads((W/'before.json').read_text());repairs=json.loads((W/'repairs.json').read_text());fixed={r['number']:r for r in repairs}
record_path=ROOT/'icon_set/model/contracts/spacing-reviews.v1.json';records=json.loads(record_path.read_text());out=[]
blocked={183:'The camera, lens and surrounding speech bubble do not have room for the required nested clearances. Keeping all three at this stroke leaves certified spacing failures.',451:'Seven repeated-orbit widths were tested. Six enclosed orbit counters remain undersized; the React geometry is retained as an unresolved candidate.',472:'The three M stems are only6 apart; they need8. Widening them inside the roundel also requires room for the L and the outer ring.',481:'The three M stems are only6 apart; the frame and L also crowd them. The complete nested brand geometry does not fit the required clearances.',493:'All six letters cannot retain their current identity and counters at this width and stroke. M stems, joined letter loops and small counters remain blocking.',494:'The TALK lettering crowds the speech bubble; the A counter and K/bubble pocket also fail. Retain the full wordmark for redesign.',498:'The N stems, E bars and speech bubble are too tightly packed for the required spacing. Keep the full LINE lettering flagged.',500:'The nested frame, speech bubble and LINE lettering cannot fit the current required spacing; letter stems and counters remain blocking.'}
notes={31:'The ring-ended branches read clearly; the short branch-to-ring approach is local to its attachment.',37:'The earring hook joins the jewel; the curved return remains a legible small attachment.',139:'The curling wave is one continuous subject; retain its overlapping crest and clear central opening.',144:'The returning alien tail remains a readable part of the continuous creature silhouette.',177:'Hair grows from the head sides; preserve the deliberate local overlap with the round head.',208:'The gear hub and branch bus form one joined hierarchy symbol.',225:'The heart bloom, leaf and stem form a readable plant; retain the local leaf attachment.',226:'Retain the matched leaf-to-stem transitions beneath the heart bloom.',254:'The hanging central bead and necklace line remain legible as one attached piece of jewelry.',295:'The rose cup and stem form one continuous flower, with an open lower vase.',308:'The loop handle joins the bag side at its intended receiver.',348:'The wings and abdomen share the insect body; preserve the readable wing divisions.',376:'The vine leaves intentionally meet the central stem; their counters remain open.',433:'The head and back form the continuous sloth silhouette; the hanging limbs remain legible.',448:'The koala head and ear are locally joined; preserve the large face opening.',455:'The cake layers share their horizontal boundaries; retain the scalloped icing.',460:'The couple topper and icing remain readable above the shared cake tiers.',483:'The three candle cups share the central upright; their equal curves remain clear.',486:'The lantern caps and suspension stems form attached ink; the larger lantern interiors remain open.',496:'The barn doors share the lamp rim; their local rim approaches preserve the spotlight construction.'}
for number,row in enumerate(before,1):
 repair=fixed.get(number);ident=repair['icon_id'] if repair else row['id'];q=json.loads((W/('repaired-qa' if repair else 'qa')/(ident+'.json')).read_text());key='solo/'+ident
 if number in blocked:
  assert q['status']=='fail',(number,q['status']);records['icons'].pop(key,None)
  out.append(dict(number=number,icon_id=row['id'],selected=ident,file=repair['file'] if repair else row['file'],decision='blocked',reason=blocked[number],errors=q['errors']));continue
 assert not q['errors'] and q['negative_space']['status']=='pass',(number,q['errors'])
 findings=q['internal_spacing']['findings'];pairs=[f['elements'] for f in findings]
 reason=(repair['reason'] if repair else notes.get(number,f"Preserve the {row['id'].replace('-',' ')} silhouette and its readable internal detail. The flagged local approaches remain legible in the centerline and native light/dark views."))
 evidence=f'icon_set/work/solo-remaining-repair/repairs-{repairs.index(repair)//20+1}.png' if repair else f'icon_set/work/solo-remaining-repair/centerlines-{(number-1)//20+1}.png'
 if findings:
  reason+=' Retain the remaining sampled opposing-edge advisories after visual review; the certified model, holes and authored-stroke pinch gate pass.'
  records['icons'][key]=dict(svg_sha256=q['svg_sha256'],rules_sha256=q['rules_sha256'],elements=pairs,reviewer=AUTHOR,reason=reason,evidence=evidence,reviewed_at='2026-09-15',hole_recheck='Authored-stroke pinch check and enclosed-hole gate both passed.')
 if repair:records['icons'].pop('solo/'+row['id'],None)
 out.append(dict(number=number,icon_id=row['id'],selected=ident,file=repair['file'] if repair else row['file'],decision='reconstructed' if repair else 'reviewed',reason=reason,evidence=evidence,reviewed_pairs=pairs,svg_sha256=q['svg_sha256']))
record_path.write_text(json.dumps(records,indent=2)+'\n');(W/'review-decisions.json').write_text(json.dumps(out,indent=2)+'\n')
from collections import Counter
print(Counter(x['decision'] for x in out))
