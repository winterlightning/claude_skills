# Remaining side-sub drawings — uniform 4px

All 15 SVGs use a 32 x 32 canvas and uniform 4px strokes, verified in each emitted SVG. No thin-stroke exceptions remain. Source modules, SVGs, metadata, previews and download archive are synchronized.

All source component paths are retained, but dense text and skull details merge at this stroke width. These drawings remain for review and are not strict SUB32 passes. QA: 14 fail, 1 review; see each full-qa.json for exact findings.

Gallery publication remains blocked by the existing missing-parent error for arrow-bottom-symbol-sub32-v3.

Open index.html for comparisons. Each UUID directory contains its SVG, Python original, reference, previews and result.json.
