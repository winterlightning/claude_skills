# Text sub-icon audit

Reviewed all 339 text artworks. 12 have confirmed geometry or sizing issues; 27 need attention including small-size visual concerns. All 339 pass 32-unit ink height, integer path-point coordinates and unclipped bounds. This is an audit, not approval.

- **404 Page Not Found Error**: Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **Air Quality Index Ten**: Arc radii remain fractional. SVG radius correction can reintroduce fractions after endpoints are rounded. Reconstruct these arcs from shared grid geometry; check their silhouettes afterward. Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **Bike Lane Text Sign**: Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **Duty Free Shopping Sign**: Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **Eight Kilometers Distance Indicator**: Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **Fifty Percent Symbol**: Arc radii remain fractional. SVG radius correction can reintroduce fractions after endpoints are rounded. Reconstruct these arcs from shared grid geometry; check their silhouettes afterward.
- **Five Hundred Internal Server Error**: Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **For Rent Text**: Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **For Sale Text Label**: Small openings change in the 4× raster but not at 16×. This is a fragile small-size detail; inspect at actual size before approving. Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **Four Meters Height Limit**: Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **Horizontal Three Dots More Options**: Dots were enlarged to a 32-unit stroke and 352-unit width. This is an incorrect text-height special case. Treat the three-dot menu mark as a symbol under icon sizing rules.
- **Internal Server Error Alert**: Arc radii remain fractional. SVG radius correction can reintroduce fractions after endpoints are rounded. Reconstruct these arcs from shared grid geometry; check their silhouettes afterward.
- **Mercury Chemical Symbol**: Snapping changes connected parts / enclosed openings, confirmed at 16×: [3, 4] → [3, 3]. Restore openings and separation with glyph-aware grid fitting.
- **No Outlet Traffic Sign**: Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **One Day Time Duration**: Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **PM 2.5 Air Quality**: Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **Seven Days Duration Text**: Small openings change in the 4× raster but not at 16×. This is a fragile small-size detail; inspect at actual size before approving. Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **Seventy Percent Symbol**: Arc radii remain fractional. SVG radius correction can reintroduce fractions after endpoints are rounded. Reconstruct these arcs from shared grid geometry; check their silhouettes afterward.
- **Share the Road Typography**: Snapping changes connected parts / enclosed openings, confirmed at 16×: [12, 0] → [10, 0]. Restore openings and separation with glyph-aware grid fitting. Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **Silver Chemical Element Symbol**: Snapping changes connected parts / enclosed openings, confirmed at 16×: [3, 5] → [3, 3]. Restore openings and separation with glyph-aware grid fitting.
- **Thirty Days Duration Indicator**: Small openings change in the 4× raster but not at 16×. This is a fragile small-size detail; inspect at actual size before approving. Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **Thirty Percent Discount Symbol**: Arc radii remain fractional. SVG radius correction can reintroduce fractions after endpoints are rounded. Reconstruct these arcs from shared grid geometry; check their silhouettes afterward.
- **Triple Z Sleeping Symbol**: Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **Two Point Five Meter Height Limit**: Snapping changes connected parts / enclosed openings, confirmed at 16×: [3, 0] → [3, 1]. Restore openings and separation with glyph-aware grid fitting. Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **Underlined Letters Rg**: Snapping changes connected parts / enclosed openings, confirmed at 16×: [3, 5] → [3, 4]. Restore openings and separation with glyph-aware grid fitting.
- **Wrench Size 24**: Visual concern: stacked text or symbols are too dense at a 32-unit total height with a 4-unit stroke. Reflow into a single line where meaning permits, using the available free width; otherwise revise the layout individually.
- **Zero Percent Sign**: Arc radii remain fractional. SVG radius correction can reintroduce fractions after endpoints are rounded. Reconstruct these arcs from shared grid geometry; check their silhouettes afterward.