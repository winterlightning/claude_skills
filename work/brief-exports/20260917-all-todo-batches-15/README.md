# TODO icon briefs — batches of 15

Exported 1,523 TODO icons with saved briefs from the live gallery. The requested count was 1,512; the live snapshot contained 11 more, all included.

There are 102 batch folders: 101 contain 15 brief files each; the last contains 8. Each batch has its reference SVG copies in `references/`. Brief text is preserved unchanged under the export metadata.

Open `copy-prompts.txt`, copy the prompt for the batch you want, and paste it into a new generation task. Every prompt already contains its repository-relative folder path and runs from the repository root. Alternatively, replace `{batch_folder}` in `prompt-template.txt` with any batch folder's repository-relative path.

Families: 1,416 solo; 68 container; 39 sub. The generation prompts use $icon-solo for every brief at 48×48, overriding the saved family when different. Original brief metadata is preserved. Generate every brief, including duplicates, previously completed or skipped items, and briefs containing older hold instructions. Create separate uniquely identified results while preserving existing icons; duplicate review will happen later. This export does not review or approve brief content.

`manifest.json` records every source UUID, family, brief, reference path, and content checksum. Gallery ordering is preserved. No gallery status was changed and no icons were generated by this export.
