# Manual fix request tasks (on hold)

Not synced with TodoPusher. When ready, copy these lines into the `## Queued`
section of `TASKS.md`. 307 manual-fix-request icons, 16 tasks of 20.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.

- [ ] $primitive-fix-thuan 20 --offset 0 --disapprove-status manual-fix-request --worker thuan-mac
  If you think an icon needs to be exceptional, make it an exception, but make sure
  the icon quality is still good for UI/UX.
